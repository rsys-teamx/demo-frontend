const USER = "user";
const AUTH_TOKEN = "authToken";
const SECOND = 1000;
const MINUTE = SECOND * 60;
const TOKEN_TTL = MINUTE * 58;
const IDLE_TIMEOUT_MIN = MINUTE * 10;
export { USER, AUTH_TOKEN, TOKEN_TTL, IDLE_TIMEOUT_MIN };